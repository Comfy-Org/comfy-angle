{
  "private": true,
  "description": "Helper scripts for downloading ANGLE libraries from Electron",
  "scripts": {
    "download": "node download.js",
    "build": "python3 build.py"
  },
  "dependencies": {
    "@electron/get": "^3.1.0",
    "yauzl": "^3.2.0"
  }
}
